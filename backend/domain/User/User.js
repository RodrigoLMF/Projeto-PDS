

class User {
    constructor(id, login, password, bills) {
      this.id = id;
      this.login = login;
      this.password = password;
      this.bills = bills;
    }
    
    
}

module.exports.User = User;