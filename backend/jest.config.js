module.exports = {
    testEnvironment: 'node', // Pode ser 'jsdom' para projetos do lado do cliente
    roots: ['<rootDir>/backend/__tests__'],
    testMatch: ['**/*.test.js'], // Padrão dos arquivos de teste
    // Outras configurações do Jest, se necessário
  };
  